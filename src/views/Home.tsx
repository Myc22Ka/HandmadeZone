import DefaultLayout from '@/layouts/DefaultLayout';
import React from 'react';

const Home: React.FC = () => {
    return (
        <DefaultLayout>
            <div>hi</div>
        </DefaultLayout>
    );
};

export default Home;
